# Supported language pairs for translation (source_lang, target_lang)
SUPPORTED_LANGUAGE_PAIRS = [
    ("en", "zh"),
    ("zh", "en"),
]

# Default source and target languages
DEFAULT_SOURCE_LANG = "en"
DEFAULT_TARGET_LANG = "zh"
